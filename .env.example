# Database Configuration
POSTGRES_USER=rhds_user
POSTGRES_PASSWORD=rhds_password
POSTGRES_DB=rhds_monorepo
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Optional: pgAdmin Configuration
PGADMIN_EMAIL=admin@rhds.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# Project-specific schema (set in each project's .env)
# PROJECT_SCHEMA=user_service
# PROJECT_SCHEMA=order_service
# etc...